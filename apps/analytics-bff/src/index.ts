// apps/analytics-bff — placeholder.
//
// Real handler written in step 14: Hono on Cloud Run, Identity Platform
// auth, GET /analytics/{code} returns click count for owner only;
// Pub/Sub listener materializes clicks/{code} from click.recorded.
export {};
