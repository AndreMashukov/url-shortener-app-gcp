// apps/redirect-bff — placeholder.
//
// Real handler written in step 12-13: Hono on Cloud Run, anonymous,
// reads lean_view/{code} and 302-redirects; also publishes
// click.recorded (sole-producer exception per BRAINSTORM §1).
export {};
