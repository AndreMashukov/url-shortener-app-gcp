// apps/app-bff — placeholder.
//
// Real handler is written in step 10 of the build plan. The v0 shape
// is Hono on Cloud Run, Identity Platform auth, POST /shorten writes
// to Firestore mappings/{code}.
export {};
